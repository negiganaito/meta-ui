import { createContext } from 'react';

import GeoPrivateDefaultTheme from '../business/geo-ui/GeoPrivateDefaultTheme';

const GeoPrivateThemeContext = createContext(GeoPrivateDefaultTheme);

export default GeoPrivateThemeContext;
